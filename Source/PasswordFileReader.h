#ifndef VARIABLE
#define VARIABLE

#include <iostream>
#include <vector>
#include <string>
#include <fstream>

//Param1 - the name of the file to open
//Opens a file and returns its lines on a vector
std::vector <std::string> getPasswords(std::string filename);

#endif